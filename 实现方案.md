# 个人作品集网站 · 实现方案

- 内容来源：`profile.md`（李星 · 智能网联汽车技术 · 2026 届应届生）
- 技术约束：原生 HTML / CSS / JavaScript，不使用前端框架、不使用第三方 UI 组件库
- 目标：桌面端 + 移动端自适应，杂志式作品展示排版，结构便于后续持续加项目

---

## 一、定位与读者

| 读者 | 关注点 | 页面需要回答的问题 |
| --- | --- | --- |
| 老师 | 教学成果、项目真实性 | 做了什么、做到了什么程度 |
| 同学 | 技术路线、可复用经验 | 用了什么技术、难点怎么解决 |
| 招聘人员 | 岗位匹配度、工程化能力 | 技能栈、项目规模、交付质量 |

结论：不走"简历式条目罗列"，走"作品集 / 杂志专题"的叙事——首屏用一句话立住定位，项目区用细节证明能力。

## 二、页面整体结构（单页 + 锚点导航）

| 顺序 | 区块 | 内容 | 排版要点 |
| --- | --- | --- | --- |
| 0 | 顶部导航 | 姓名标识 + 首页 / 作品 / 关于 / 联系 | 通栏细线分隔，滚动后吸顶；移动端折叠为菜单按钮 |
| 1 | 首屏 Hero | 姓名、一句话定位、关键词、求职状态 | 超大字标题 + 字重/字号强对比 + 一条细分割线 + 一处局部色块；右侧小字放城市 / 状态 |
| 2 | 作品精选（核心） | 6 个项目的完整展示 + 类别筛选 | 12 列网格上的多种版式交替，编号 + 分割线串联 |
| 3 | 关于我 | 自述段落、技能分组、荣誉 / 时间线 | 左右两栏：左为叙述文字，右为技能分组清单（细线分隔，不用卡片） |
| 4 | 联系方式 | 邮箱、电话、城市、社交链接 | 邮箱用大字号做视觉收尾，链接排成一行小标签 |
| 5 | 页脚 | 版权、技术说明 | 极简，一行小字 |

语义化标签：`header` / `main` / `section` / `article` / `footer`，标题层级 h1 → h3 严格递进。

## 三、项目区域排版（本次设计的重点）

### 3.1 底层骨架
用 CSS Grid 12 列作为统一栅格，行与行之间用细分割线（hairline）+ 项目编号（01 / 02 …）串联，形成"杂志目录"的连续感。**不使用等高卡片、不使用统一网格**。

### 3.2 四种版式变体

| 变体 | 视觉特征 | 占位方式 | 适用项目 |
| --- | --- | --- | --- |
| `feature` | 通栏大图在上，编号 + 标题 + 说明在下，字号最大 | 12 列满宽 | 最想让人先看到的重点项目（智能座舱实训系统） |
| `split-left` | 大图在左、文字在右，图文约 3:2 | 图 7 列 / 文 5 列 | 过程性、需大量文字说明的项目（ADAS 仿真平台） |
| `split-right` | 与上者镜像，文字在左、图在右 | 文 5 列 / 图 7 列 | 与上一个形成视线往返（工业级 AGV 底盘） |
| `editorial` | 小图 + 超大序号 + 大量留白，文字错位排布，图片尺寸明显收小 | 图 3 列 / 文 6 列，其余留白 | 需要换气、强调叙述的项目（巡线小车、击剑评分系统） |

**节奏规则**：连续两个"文字为主"的版式之间必须插入一个 `feature`，避免整屏都是小图小字；整体保持"大图 → 分栏 → 分栏 → 留白"的呼吸频率。

### 3.3 每个项目的固定信息单元
顺序固定、样式随变体变化：编号 → 类别标签（局部色块）→ 名称 → 简介 → 技术栈（小字，`·` 分隔）→ 完成时间 → 配图。

### 3.4 类别筛选
作品区顶部一行筛选标签（全部 / 智能座舱 / 自动驾驶 / 机器人系统 / 视觉算法），点击按 `category` 过滤。实现上只用 class 切换控制显隐，不重建 DOM，结构不变形。

### 3.5 移动端处理
所有变体在窄屏统一折叠为单列：图在上、文字在下，保留编号与分割线以维持杂志感；项目编号与类别标签同排，避免"每个项目一张卡"的观感。

## 四、视觉风格

| 项 | 方案 |
| --- | --- |
| 底色 | 米白 / 极浅灰（非纯白），项目图片区域留白更足 |
| 文字色 | 近黑深灰做正文，深墨色做大标题，弱灰做注释小字 |
| 强调色 | 单一冷色（钴蓝系）用于编号、类别色块、链接下划线；不做多色堆叠 |
| 字体 | 标题用衬线/宋体类（中文杂志感），正文用系统无衬线；字号用 `clamp()` 随视口缩放 |
| 结构手段 | 细分割线、留白、局部色块、序号错位——代替阴影和卡片 |
| 动效 | 仅两处：图片 hover 轻微缩放/位移、滚动淡入（IntersectionObserver，只触发一次，遵守 `prefers-reduced-motion`） |

## 五、文件组织

```
lab03/
├── index.html              页面骨架 + 语义化区块（不含项目数据）
├── css/
│   ├── base.css            设计变量（配色/字号/间距/栅格）、reset、基础排版
│   ├── layout.css          导航、区块容器、栅格、关于我、联系、页脚
│   ├── projects.css        四种版式变体、项目编号、分割线、类别标签
│   └── responsive.css      断点适配（≥1200px / 768–1199px / <768px）
├── js/
│   ├── projects.js         项目数据数组（唯一需要日常维护的文件）
│   ├── render.js           按 layout 字段生成对应 DOM
│   ├── interactions.js     类别筛选、移动端菜单、滚动淡入
│   └── main.js             入口，串联以上模块
├── assets/
│   └── images/             项目配图 project-01.jpg … project-06.jpg（先用占位图）
└── 实现方案.md              本文档
```

## 六、数据结构与扩展方式

`projects.js` 中每个项目一个对象：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | `p01` 形式，同时作为锚点 |
| `title` | string | 项目名称 |
| `category` | string | 类别，用于筛选与色块文案 |
| `period` | string | 完成时间，如 `2026.07` |
| `summary` | string | 简介，1–2 句 |
| `tech` | string[] | 技术栈 |
| `image` | string | 配图路径 |
| `layout` | string | `feature` / `split-left` / `split-right` / `editorial` |
| `links` | object | 可选：演示地址 / 仓库地址 |

**新增一个项目只需三步**：① 往 `projects.js` 追加一条对象并指定 `layout`；② 把配图放进 `assets/images/`；③ 刷新页面。HTML 与 CSS 无需改动（`index.html` 里不写死任何项目）。

## 七、响应式断点

| 断点 | 布局 |
| --- | --- |
| ≥1200px | 12 列完整版式，左右错落、留白最大化 |
| 768–1199px | 网格收窄为 8 列，分栏变体压缩为图 6 / 文 2 或直接上下堆叠 |
| <768px | 全部单列；导航折叠为菜单；编号与类别标签同排；图片按固定宽高比显示 |

图片统一用 `aspect-ratio` + `object-fit: cover`，避免移动端拉伸变形与横向溢出。

## 八、实施步骤

1. 搭 `index.html` 骨架与语义结构，区块留空占位
2. 写 `base.css` 设计变量 + reset + 基础排版，先定首屏 Hero
3. 写 `projects.js` 数据（用 `profile.md` 的 6 个项目）+ `render.js` 渲染
4. 写 `projects.css` 四种版式变体，调排版节奏与分割线
5. 补 `interactions.js`：筛选、移动端菜单、滚动淡入
6. 写 `responsive.css`，逐断点校正；替换真实配图，自测验收

## 九、验收自测清单

- [ ] 1440 / 1280 / 768 / 375 四个宽度下均无横向滚动条、无文字溢出
- [ ] 六个项目全部完整显示，各自版式符合预期且整体风格统一
- [ ] 导航锚点跳转正确，移动端菜单可开可关
- [ ] 类别筛选后布局不错位、留白不塌陷
- [ ] 图片比例一致、无拉伸；hover 效果不引起布局抖动
- [ ] 键盘 Tab 可遍历导航与链接，焦点可见；图片均有 `alt`

## 十、待确认事项

1. **配图**：是否已有项目截图？没有的话先用占位图（同色系灰块 + 项目名），后续直接替换。
2. **强调色**：默认钴蓝系，如果你更想用别的单一强调色，告诉我色值。
3. **是否部署**：需要上线（GitHub Pages / 静态托管）还是本地交付即可。
4. **关于我**：荣誉、奖项是否要单独成块，还是并入"关于我"。
